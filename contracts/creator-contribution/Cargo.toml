[package]
name = "creator-contribution"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "=25.0.0"

[dev-dependencies]
soroban-sdk = { version = "=25.0.0", features = ["testutils"] }