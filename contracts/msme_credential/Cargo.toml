[package]
name = "msme-credential-contract"
version = "0.1.0"
edition = "2021"
authors = ["ByahéBITES Team <jugawayne@gmail.com>"]
license = "Apache-2.0"
description = "Soroban smart contract for LGU-verified MSME Credential issuance on Stellar"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
